RewriteEngine On

# Remove index.html from URL
RewriteCond %{THE_REQUEST} /index\.html[\s?] [NC]
RewriteRule ^index\.html$ / [R=301,L]

# Remove contact.html — redirect to /contact
RewriteCond %{THE_REQUEST} /contact\.html[\s?] [NC]
RewriteRule ^contact\.html$ /contact [R=301,L]

# Serve index.html for clean section paths
RewriteRule ^(about|services|niches|portfolio|why-us)$ index.html [L]

# Serve contact.html for /contact
RewriteRule ^contact$ contact.html [L]
